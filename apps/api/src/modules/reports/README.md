# Reports Module

Placeholder module. Future implementation will expose endpoints to
generate aggregated reports across projects/clients.

Currently the `/reports` route returns `501 Not Implemented`.
